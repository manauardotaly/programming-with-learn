# programming-store
Personal data...

## PHP connect
Use this to quickly test a MySQL connection from the CLI:

```bash
DB_HOST=127.0.0.1 DB_PORT=3306 DB_NAME=my_db DB_USER=root DB_PASS=secret php connect.php
```

